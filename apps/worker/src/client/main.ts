// Placeholder stub — the LIFF flow lives in LINE Harness, not here. The
// admin UI is deployed separately to CF Pages. This file exists only so
// vite's build step doesn't choke on the leftover index.html reference.
export {};
