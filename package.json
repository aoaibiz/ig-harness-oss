{
  "name": "ig-harness",
  "version": "0.11.1",
  "private": true,
  "description": "Open-source Instagram DM CRM/marketing automation",
  "scripts": {
    "dev:worker": "pnpm --filter worker dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "guard:prepublish": "bash scripts/pre-publish-guard.sh",
    "deploy:worker": "pnpm --filter worker deploy",
    "deploy:web": "pnpm --filter web build",
    "db:migrate": "wrangler d1 execute instagram-harness --remote --file=packages/db/schema.sql && node scripts/apply-migrations.mjs --remote",
    "db:migrate:local": "wrangler d1 execute instagram-harness --local --file=packages/db/schema.sql && node scripts/apply-migrations.mjs --local",
    "deploy:setup": "node packages/create-ig-harness/dist/index.js",
    "deploy:update": "node packages/create-ig-harness/dist/index.js update"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "4"
  }
}
